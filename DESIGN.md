# دليل تصميم «البشري» — الأساس الموحّد لفروع الموقع

هذا الدليل يوثّق طريقة التصميم المستخرجة من صفحة **نتيجة الشهادة الثانوية للولاية الشمالية 2026**،
ليُبنى عليها كل فرع جديد في الموقع بنفس الهوية: نفس الألوان، نفس العناوين، نفس الأشكال،
نفس الإهداء، ونفس عدّاد الزيارات.

---

## 1. الملفات المشتركة

| الملف | الوظيفة |
|---|---|
| `shared/theme.css` | نظام التصميم كاملاً (15 قسماً مرقّماً بالتعليقات) |
| `shared/fonts.css` | خطَّا **Tajawal** و**Amiri** مستضافان محلياً — لا حاجة لإنترنت |
| `shared/fonts/` | ملفات الخطوط woff2 (عربي + لاتيني) |
| `shared/site.js` | أدوات جاهزة: رمز التحقق، عدّاد الزيارات، تنسيق الأرقام، الطباعة والمشاركة |
| `shared/assets/albashari-logo.png` | الشعار الكامل — للترويسة والتذييل |
| `shared/assets/albashari-mark.png` | الختم — للعلامة المائية داخل اللوحة وللأيقونة |
| `template/index.html` | قالب صفحة كامل وجاهز للنسخ |

**لبدء فرع جديد:** انسخ مجلد `template/` باسم الفرع، ثم غيّر العنوان والبيانات فقط.

```bash
cp -r template/ natija-2027/
```

---

## 2. الهوية اللونية

| المتغيّر | اللون | الاستخدام |
|---|---|---|
| `--navy-900` | `#0B1A38` | خلفية الصفحة |
| `--navy-800` / `--navy-700` / `--navy-600` | كحلي متدرّج | الأزرار، الأعمدة، رؤوس الجداول |
| `--gold` / `--gold-lt` / `--gold-dp` | `#C6A35F` · `#E7CE92` · `#9C7B3C` | الإطارات والعناوين |
| `--cream` | `#F8F4E9` | خلفية اللوحة (ورق الشهادة) |
| `--cyan` | `#2BC4D8` | لمسة من دوائر الشعار + حلقة التركيز |
| `--ok` / `--no` / `--warn` | أخضر · أحمر · عنبري | حالات النتيجة |

**قاعدة ثابتة:** الخلفية كحلية داكنة، والمحتوى المهم داخل لوحة كريمية بإطار ذهبي.
هذا التباين هو ما يعطي الصفحة شكل «الشهادة الرسمية».

---

## 3. الخطوط

- **العناوين** بخط `Amiri` (`--serif`) — رسمي وعربي الطابع.
- **النصوص والأزرار** بخط `Tajawal` (`--sans`) — واضح على الجوال.
- كل المقاسات تستخدم `clamp()` لتتكيّف مع الشاشة تلقائياً، بلا نقاط توقّف إضافية.

---

## 4. ترتيب الصفحة (لا يتغيّر بين الفروع)

1. **الترويسة** — لوحة بيضاء تحمل الشعار، ثم خط ذهبي بمعيّن في وسطه، ثم `h1`، ثم سطر وصف.
2. **لوحة الإدخال** `.panel` — عنوان `.lead`، الحقول، رمز التحقق، زر `button.go` عريض.
3. **بطاقة النتيجة** `.panel#result` — مخفية حتى يتم الاستعلام، وتحتوي:
   - `.rhead` الاسم + شارة الحالة `.badge`
   - `.meta` شبكة بيانات (**استخدم عدداً زوجياً من الخلايا** حتى لا تبقى خانة فارغة)
   - `.pillars` ثلاثة أعمدة كحلية بأرقام ذهبية كبيرة
   - جدول التفاصيل، ثم `.note` للملاحظات، ثم أزرار `.acts`
4. **الإهداء** `.ded` — فقرتان بلغة قريبة من القلب، وتوقيع `.sig`.
5. **عدّاد الزيارات** `.stats` — يظهر فقط عند توفّر الخادم.
6. **التذييل** — الشعار ثانيةً، «هدية مقدمة من أبو جنان»، اسم الشركة، ثم سطر إخلاء المسؤولية.

كل قسم لا يُطبع يأخذ الصنف `no-print`.

---

## 5. الأدوات الجاهزة في `site.js`

```js
Site.fmtNum(12345)              // "12,345"
Site.normalizeDigits("١٠٠١")    // "1001"  (يقبل العربية والفارسية)
Site.el(tag, cls, txt)          // إنشاء عنصر
Site.cell("المدرسة", "ثانوية دنقلا")   // خلية لشبكة .meta
Site.pillar("90.0%", "النسبة")          // عمود من .pillars

var msg = Site.message("msg");  msg.fail("..."); msg.good("..."); msg.clear();

var cap = Site.captcha({ canvas:"cap", reload:"newcap", input:"code" });
cap.verify(value);  cap.redraw();

Site.print();
Site.share("نص المشاركة");
Site.visits({ box:"visits", endpoint:"/api/visits" });
```

---

## 6. عدّاد الزيارات

الصندوق مخفي افتراضياً (`style="display:none"`) ولا يظهر إلا إذا ردّ الخادم بنجاح،
فلا تظهر أرقام مكسورة على الاستضافة الساكنة.

**الطلب:** `GET /api/visits?m=hit|ping&sid=<معرّف الجلسة>`
`hit` عند فتح الصفحة، و`ping` كل 45 ثانية لتحديث «المتصلون الآن».

**الرد المتوقّع:**

```json
{
  "ok": true,
  "total": 128430,
  "online": 37,
  "countries": [ { "code": "SD", "n": 91200 }, { "code": "SA", "n": 14100 } ]
}
```

`code` رمز الدولة بحرفين (ISO-3166) — والصفحة تحوّله إلى علم واسم عربي تلقائياً
(قائمة الأسماء في `Site.countryNames`، والعلم عبر `Site.flagEmoji`).
الدول تُعرض مرتّبة كما يرسلها الخادم، وأول 12 فقط.

> على Netlify ضع الدالة في `netlify/functions/visits.js` مع إعادة توجيه
> `/api/visits` إليها، واقرأ رمز الدولة من الترويسة `x-nf-geo`.
> على Cloudflare Pages استخدم `functions/api/visits.js` واقرأ `request.cf.country`.

---

## 7. قواعد ثابتة لكل فرع

- اللغة عربية و`dir="rtl"` دائماً.
- لا روابط خارجية ولا إعلانات، وكل الأصول محلية حتى تعمل الصفحة بلا إنترنت.
- كل صفحة تعمل بملف واحد + المجلد المشترك، بلا خطوات بناء ولا مكتبات.
- الأزرار الكبيرة والحقول العريضة — الاستخدام الأساسي من الجوال.
- تُختبر الطباعة قبل النشر: القسم الأبيض فقط يُطبع، والباقي `no-print`.
- سطر إخلاء المسؤولية في التذييل إلزامي عند عرض بيانات رسمية.

---

## 8. بيانات الأشخاص

بيانات الطلاب والنتائج **لا تُحفظ في المستودع**. تُقرأ من ملف بيانات منفصل
أو من الخادم وقت التشغيل. القالب هنا يحمل سجلّين تجريبيين فقط لعرض الشكل.
